#!/usr/bin/env python3
"""Hermes Agent CLI launcher."""

import subprocess
import sys


if __name__ == "__main__":
    if len(sys.argv) >= 2 and sys.argv[1].lower() == "os":
        raise SystemExit(subprocess.call(["hermes-os"] + sys.argv[2:]))

    from hermes_cli.main import main
    main()
